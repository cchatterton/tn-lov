{
  "version": "2.0.2",
  "body": "Fixes legacy-plugin coexistence by registering compatibility functions only after all plugin files have loaded, preventing function redeclaration regardless of plugin load order."
}
